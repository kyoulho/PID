{
  "compilerOptions": {
    // import 시에 정적 경로 사용 가능
    "baseUrl": "src",
    // 컴파일된 파일을 저장할 디렉토리
    "outDir": "./dist",
    // 소스 파일의 루트 디렉토리
    "rootDir": "./src",
    // 데코레이터 사용을 허용
    "experimentalDecorators": true,
    // 데코레이터 메타데이터를 런타임에 생성
    "emitDecoratorMetadata": true,
    // 타입 정의 파일(.d.ts)을 생성
    "declaration": true,
    // 타입 정의 파일에 대한 소스맵 생성
    "declarationMap": true,
    // 소스맵 생성
    "sourceMap": true
  },
  "include": [
    "src/**/*",
  ],
  "exclude": [
    "node_modules",
    "dist"
  ]
}