{
  "compilerOptions": {
    "outDir": "./dist",
    // 컴파일된 파일을 저장할 디렉토리
    "rootDir": "./src",
    // 소스 파일의 루트 디렉토리
    "experimentalDecorators": true,
    // 데코레이터 사용을 허용
    "emitDecoratorMetadata": true,
    // 데코레이터 메타데이터를 런타임에 생성
    "baseUrl": "src",
  },
  "include": [
    "src/**/*",                   // src 디렉토리 하위 파일 포함
    "src/index.ts"
    // 루트에 있는 index.ts를 포함
  ],
  "exclude": [
    "node_modules",               // 빌드에서 제외
    "dist"                        // 빌드된 파일 제외
  ]
}